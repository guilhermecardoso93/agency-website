import styled, { keyframes } from "styled-components";


const HomeSection = styled.section``;

const HeroSection = () => {
  return <HomeSection id="home"></HomeSection>;
};

export default HeroSection;
